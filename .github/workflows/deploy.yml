name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ 34.118.74.50 }}
          username: ${{ tsap.ihor }}
          key: ${{ ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPfpqzRTQMaltiYfvFDGP1+O/2SGXTb8WZwW4S+q/F28 tsap.ihor@gmail.com }}
          script: |
            cd ~/nonograms
            git pull origin main
            docker compose build --no-cache
            docker compose up -d